{
  "name": "hardhat",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat deploy",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "copy-artifacts": "mkdir -p ../nextjs/src/contracts && cp -r artifacts/contracts ../nextjs/src/contracts/ 2>/dev/null || true && cp -r typechain-types ../nextjs/src/contracts/ 2>/dev/null || true"
  },
  "devDependencies": {
    "@fhenixprotocol/cofhe-contracts": "0.0.13",
    "@fhenixprotocol/cofhe-mock-contracts": "^0.2.1-alpha.0",
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.0.0",
    "cofhe-hardhat-plugin": "^0.2.1-alpha.1",
    "dotenv": "^16.4.5",
    "fhenix-confidential-contracts": "github:FhenixProtocol/fhenix-confidential-contracts#erc20-confidential-v2",
    "hardhat": "^2.22.0",
    "hardhat-deploy": "^0.12.0",
    "typescript": "^5.0.0"
  }
}
